{
  "name": "json-server",
  "version": "1.0.0",
  "scripts": {
    "start": "json-server --host 0.0.0.0 --port $PORT --watch db.json"
  },
  "dependencies": {
    "json-server": "^0.16.3"
  },
  "engines": {
    "node": ">=14.x"
  }
}
